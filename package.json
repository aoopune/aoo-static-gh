{
  "name": "applyonlyonce",
  "version": "1.0.0",
  "description": "Education loan comparison - Apply Only Once",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8765",
    "test": "node tests/run-unit.js 2>/dev/null || true",
    "test:ui": "npx playwright test",
    "test:deployed": "BASE_URL=${BASE_URL:-https://YOUR_USERNAME.github.io/applyonlyonce/} npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "playwright": "^1.40.0"
  }
}
