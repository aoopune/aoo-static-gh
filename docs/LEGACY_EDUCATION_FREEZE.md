# Legacy education stack — frozen

Education-loan pages remain in the repo but must not appear in redesigned UI.

- Do not link them from redesigned nav, footer, sitemap, or home.
- Do not rewrite them “for cleanup” unless re-enabling the product.
- Markers: `EDUCATION_LOAN_PRODUCT`, `LEGACY_EDUCATION_PAGES` in `partials/global-nav.html`.
- Re-enable only when explicitly asked: uncomment those blocks and restore gated links.
