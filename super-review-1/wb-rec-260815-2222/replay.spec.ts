import { test, expect } from '@playwright/test';

// Auto-generated by workbooks-recorder from bundle: recorded session (workbooks-recording/2.2)
// Recorded: 2026-08-15T16:52:14.273Z
// Duration (active): 534239ms

test("recorded session", async ({ page }) => {
  await test.step("recorded session", async () => {
    await page.goto("http://localhost:8765/pages/explore-banks.html");
    await page.screenshot({ path: "screenshots/recorded-session-start.png" });
    // unknown event kind: landmark_snapshot
    // idle 7699ms (no stable next locator — consider a manual waitFor)
    // idle 7998ms (no stable next locator — consider a manual waitFor)
    // idle 7996ms (no stable next locator — consider a manual waitFor)
    // idle 8023ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 10005ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 8001ms (no stable next locator — consider a manual waitFor)
    // idle 8001ms (no stable next locator — consider a manual waitFor)
    // idle 7999ms (no stable next locator — consider a manual waitFor)
    // idle 7974ms (no stable next locator — consider a manual waitFor)
    // idle 8027ms (no stable next locator — consider a manual waitFor)
    // idle 7998ms (no stable next locator — consider a manual waitFor)
    // idle 8004ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 7999ms (no stable next locator — consider a manual waitFor)
    // idle 7999ms (no stable next locator — consider a manual waitFor)
    // idle 9998ms (no stable next locator — consider a manual waitFor)
    // idle 7975ms (no stable next locator — consider a manual waitFor)
    // idle 8027ms (no stable next locator — consider a manual waitFor)
    // idle 7997ms (no stable next locator — consider a manual waitFor)
    // idle 7977ms (no stable next locator — consider a manual waitFor)
    // idle 8025ms (no stable next locator — consider a manual waitFor)
    // idle 9999ms (no stable next locator — consider a manual waitFor)
    // idle 10001ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 8001ms (no stable next locator — consider a manual waitFor)
    // idle 10000ms (no stable next locator — consider a manual waitFor)
    // idle 7973ms (no stable next locator — consider a manual waitFor)
    // idle 7999ms (no stable next locator — consider a manual waitFor)
    // idle 8001ms (no stable next locator — consider a manual waitFor)
    // idle 8027ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 7998ms (no stable next locator — consider a manual waitFor)
    // idle 7974ms (no stable next locator — consider a manual waitFor)
    // idle 8027ms (no stable next locator — consider a manual waitFor)
    // idle 8002ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 7998ms (no stable next locator — consider a manual waitFor)
    // idle 9992ms (no stable next locator — consider a manual waitFor)
    // idle 8007ms (no stable next locator — consider a manual waitFor)
    // idle 7974ms (no stable next locator — consider a manual waitFor)
    // idle 8022ms (no stable next locator — consider a manual waitFor)
    // idle 8006ms (no stable next locator — consider a manual waitFor)
    // idle 9965ms (no stable next locator — consider a manual waitFor)
    // idle 8034ms (no stable next locator — consider a manual waitFor)
    // idle 7967ms (no stable next locator — consider a manual waitFor)
    // idle 8030ms (no stable next locator — consider a manual waitFor)
    // idle 10001ms (no stable next locator — consider a manual waitFor)
    // idle 10002ms (no stable next locator — consider a manual waitFor)
    // idle 7999ms (no stable next locator — consider a manual waitFor)
    // idle 7969ms (no stable next locator — consider a manual waitFor)
    // idle 8042ms (no stable next locator — consider a manual waitFor)
    // idle 8009ms (no stable next locator — consider a manual waitFor)
    // idle 8010ms (no stable next locator — consider a manual waitFor)
    // idle 7976ms (no stable next locator — consider a manual waitFor)
    // idle 8032ms (no stable next locator — consider a manual waitFor)
    // idle 8000ms (no stable next locator — consider a manual waitFor)
    // idle 7968ms (no stable next locator — consider a manual waitFor)
    // idle 8036ms (no stable next locator — consider a manual waitFor)
    // idle 7997ms (no stable next locator — consider a manual waitFor)
    // idle 8001ms (no stable next locator — consider a manual waitFor)
    // idle 9973ms (no stable next locator — consider a manual waitFor)
    // idle 8027ms (no stable next locator — consider a manual waitFor)
  });
});
